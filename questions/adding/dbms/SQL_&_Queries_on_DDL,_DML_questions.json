[
  {
    "question": "Which SQL statement is used to modify the structure of an existing table?",
    "options": [
      "UPDATE",
      "ALTER TABLE",
      "MODIFY",
      "CHANGE"
    ],
    "correct_option_index": 1,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Which DML command is used to remove rows from a table based on a specified condition?",
    "options": [
      "DELETE",
      "DROP",
      "REMOVE",
      "TRUNCATE"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "What does DDL stand for in the context of databases?",
    "options": [
      "Data Definition Language",
      "Data Description Language",
      "Database Definition Language",
      "Database Description Language"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Which SQL keyword is used to add a new column to an existing table?",
    "options": [
      "INSERT",
      "ADD",
      "NEW",
      "ALTER"
    ],
    "correct_option_index": 1,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "What is the purpose of the `TRUNCATE TABLE` command?",
    "options": [
      "Delete all rows from a table",
      "Drop the table",
      "Modify the table structure",
      "Rename the table"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Which DML statement is used to insert new data into a table?",
    "options": [
      "SELECT",
      "UPDATE",
      "INSERT",
      "CREATE"
    ],
    "correct_option_index": 2,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Which clause is used with the DELETE statement to specify the rows to be deleted?",
    "options": [
      "WHERE",
      "FOR",
      "IF",
      "FILTER"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "What does DML stand for in the context of databases?",
    "options": [
      "Data Manipulation Language",
      "Data Management Language",
      "Database Manipulation Language",
      "Database Management Language"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Which SQL command is used to completely remove a table from the database?",
    "options": [
      "DELETE TABLE",
      "REMOVE TABLE",
      "TRUNCATE TABLE",
      "DROP TABLE"
    ],
    "correct_option_index": 3,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Which keyword is used to rename a column in an existing table using ALTER TABLE?",
    "options": [
      "RENAME",
      "CHANGE",
      "MODIFY",
      "UPDATE"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "What is the primary difference between DELETE and TRUNCATE TABLE?",
    "options": [
      "DELETE resets the identity, TRUNCATE doesn't",
      "TRUNCATE can be rolled back, DELETE cannot",
      "DELETE is faster than TRUNCATE",
      "TRUNCATE resets the identity, DELETE doesn't"
    ],
    "correct_option_index": 3,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Which of the following is NOT a DDL statement?",
    "options": [
      "CREATE",
      "ALTER",
      "DROP",
      "SELECT"
    ],
    "correct_option_index": 3,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "What is the purpose of the `UPDATE` statement?",
    "options": [
      "To insert new rows",
      "To delete existing rows",
      "To modify existing rows",
      "To retrieve data"
    ],
    "correct_option_index": 2,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "How do you add a `NOT NULL` constraint to an existing column using ALTER TABLE?",
    "options": [
      "ALTER TABLE table_name ADD CONSTRAINT column_name NOT NULL;",
      "ALTER TABLE table_name MODIFY column_name NOT NULL;",
      "ALTER TABLE table_name ALTER COLUMN column_name SET NOT NULL;",
      "ALTER TABLE table_name UPDATE column_name WITH NOT NULL;"
    ],
    "correct_option_index": 2,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Which SQL keyword is used to remove a column from a table?",
    "options": [
      "REMOVE",
      "DELETE",
      "DROP",
      "DELETE COLUMN"
    ],
    "correct_option_index": 2,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Consider a table 'Employees' with columns (id INT, name VARCHAR(255), salary INT).  Write the SQL statement to increase the salary of all employees by 10%.",
    "options": [
      "UPDATE Employees SET salary = salary + (salary * 0.1);",
      "MODIFY Employees SET salary = salary * 1.1;",
      "ALTER Employees SET salary = salary + 10% of salary;",
      "CHANGE Employees SET salary = salary + (salary / 10);"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "What will happen if you try to insert a NULL value into a column defined as `NOT NULL`?",
    "options": [
      "The NULL value will be automatically replaced with a default value.",
      "The insertion will succeed, and the NULL value will be inserted.",
      "An error will occur, and the insertion will fail.",
      "The NULL value will be converted to an empty string."
    ],
    "correct_option_index": 2,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Which of the following statements correctly renames the 'CustomerID' column to 'CustID' in the 'Customers' table?",
    "options": [
      "ALTER TABLE Customers RENAME COLUMN CustomerID TO CustID;",
      "ALTER TABLE Customers CHANGE COLUMN CustomerID TO CustID;",
      "ALTER TABLE Customers MODIFY COLUMN CustomerID CustID;",
      "UPDATE Customers SET CustomerID = CustID;"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "What is the difference between using `WHERE column IS NULL` and `WHERE column = NULL` in a SQL query?",
    "options": [
      "They are equivalent and will produce the same result.",
      "`WHERE column = NULL` will return rows where the column has a value of 0.",
      "`WHERE column IS NULL` is the correct way to check for NULL values; `WHERE column = NULL` will always return an empty result set.",
      "`WHERE column = NULL` is used to assign NULL to a column, not to check for it."
    ],
    "correct_option_index": 3,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Assume a table 'Orders' with 'OrderID', 'CustomerID', and 'OrderDate'.  How would you delete all orders placed before January 1, 2023?",
    "options": [
      "DELETE FROM Orders WHERE OrderDate < '2023-01-01';",
      "DELETE * FROM Orders WHERE OrderDate BEFORE '2023-01-01';",
      "REMOVE FROM Orders WHERE OrderDate < '2023-01-01';",
      "TRUNCATE TABLE Orders WHERE OrderDate < '2023-01-01';"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "What happens when you execute a DML statement without a WHERE clause?",
    "options": [
      "It will affect all rows in the table.",
      "It will not execute and will return an error.",
      "It will only affect the first row in the table.",
      "It will randomly affect a few rows in the table."
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Consider a table 'Products' with columns 'ProductID', 'ProductName', 'Price'. You want to add a new column 'Category' of type VARCHAR(50) to this table. Which SQL statement would you use?",
    "options": [
      "ALTER TABLE Products ADD Category VARCHAR(50);",
      "ADD COLUMN Category VARCHAR(50) TO Products;",
      "MODIFY TABLE Products ADD COLUMN Category VARCHAR(50);",
      "INSERT INTO Products (Category) VALUES (VARCHAR(50));"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "What is the effect of executing the SQL command `TRUNCATE TABLE my_table;`?",
    "options": [
      "It deletes the table named 'my_table'.",
      "It deletes all rows from the table 'my_table', but keeps the table structure intact.",
      "It renames the table 'my_table'.",
      "It adds a new column to the table 'my_table'."
    ],
    "correct_option_index": 1,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "You have a table 'Employees' with columns 'EmployeeID', 'Name', 'Salary'. You want to decrease the salary of all employees by 5% where their current salary is above $50,000. What's the correct SQL statement?",
    "options": [
      "UPDATE Employees SET Salary = Salary - (Salary * 0.05) WHERE Salary > 50000;",
      "MODIFY Employees SET Salary = Salary * 0.95 WHERE Salary > 50000;",
      "ALTER TABLE Employees UPDATE Salary SET Salary = Salary * 0.95 WHERE Salary > 50000;",
      "UPDATE Employees SET Salary = Salary * 0.95 IF Salary > 50000;"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Which SQL command is used to remove a constraint from a table?",
    "options": [
      "REMOVE CONSTRAINT",
      "DELETE CONSTRAINT",
      "DROP CONSTRAINT",
      "ALTER TABLE DROP CONSTRAINT"
    ],
    "correct_option_index": 3,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Consider a scenario where you need to update a 'Customers' table. However, you want to update only a maximum of 100 rows. Which vendor-specific approach is most suitable if standard SQL doesn't offer a direct 'LIMIT' clause for UPDATE statements in your database system?",
    "options": [
      "Using a cursor to iterate through rows and update the first 100.",
      "Using a subquery to select the first 100 'CustomerID' values and then updating only those rows.",
      "Using a transaction with a counter to update rows until the counter reaches 100, then rolling back the transaction.",
      "There is no way to limit the number of updated rows without a 'LIMIT' clause."
    ],
    "correct_option_index": 1,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "You're working with a large database and need to perform a DML operation that affects almost all rows in a table. Which approach would be the most efficient to minimize impact and ensure data integrity?",
    "options": [
      "Performing the operation directly on the live table during peak hours.",
      "Creating a copy of the table, performing the operation on the copy, and then switching the tables in a low-traffic window.",
      "Using a series of small, batched DML operations with short commit intervals to minimize lock contention.",
      "Truncating the table and re-inserting the modified data."
    ],
    "correct_option_index": 2,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "A developer accidentally executed `DELETE FROM Customers;` without a WHERE clause on a production database. What's the most appropriate immediate action, assuming full database backups are available?",
    "options": [
      "Immediately restore the latest full backup of the database.",
      "Attempt to reconstruct the data by analyzing transaction logs, if possible.",
      "Alert all users to the data loss and ask them to manually re-enter their information.",
      "Run an UPDATE statement to set all CustomerID values to a default value."
    ],
    "correct_option_index": 1,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "You have a table 'Logs' that grows very large. You need to delete old log entries periodically to manage disk space. However, due to auditing requirements, you can only delete logs older than one year, and the operation must not severely impact the performance of the system. What is the best approach?",
    "options": [
      "Run a single `DELETE FROM Logs WHERE LogDate < DATE('now', '-1 year');` statement during peak hours.",
      "Partition the 'Logs' table by year and drop the partition containing logs older than one year during off-peak hours.",
      "Truncate the 'Logs' table and re-insert the logs younger than one year from a backup.",
      "Disable logging for one day and then delete all logs older than one year."
    ],
    "correct_option_index": 1,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "In the context of DDL, what is the impact of using the `CASCADE` option with a `DROP TABLE` statement?",
    "options": [
      "It prevents the table from being dropped if there are any dependencies.",
      "It automatically drops all tables that have foreign key relationships with the dropped table.",
      "It backs up the table before dropping it.",
      "It prompts the user for confirmation before dropping the table."
    ],
    "correct_option_index": 1,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "You have a database with multiple tables, and you need to change the data type of a column in one table. Before doing so, what is the most important consideration to prevent data loss or application errors?",
    "options": [
      "Checking if there are any indexes on the column.",
      "Ensuring the new data type can accommodate all existing values in the column.",
      "Backing up the entire database.",
      "Making sure no one is currently using the database."
    ],
    "correct_option_index": 1,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Which of the following operations cannot be rolled back after being committed in most database systems?",
    "options": [
      "INSERT",
      "UPDATE",
      "DELETE",
      "TRUNCATE"
    ],
    "correct_option_index": 3,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Given a table 'Students' with columns 'StudentID', 'Name', and 'Major', how would you add a constraint to ensure that the 'Major' column can only contain one of the following values: 'CS', 'EE', 'ME'?",
    "options": [
      "ALTER TABLE Students ADD CONSTRAINT valid_major CHECK (Major IN ('CS', 'EE', 'ME'));",
      "ALTER TABLE Students MODIFY Major ENUM ('CS', 'EE', 'ME');",
      "ALTER TABLE Students SET Major TYPE ENUM ('CS', 'EE', 'ME');",
      "UPDATE Students SET Major IN ('CS', 'EE', 'ME');"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "You want to remove all rows from a table named 'TempData' and reset the auto-incrementing ID to its initial value. Which command is the most efficient way to achieve this?",
    "options": [
      "DELETE FROM TempData;",
      "TRUNCATE TABLE TempData;",
      "DROP TABLE TempData; CREATE TABLE TempData (...);",
      "ALTER TABLE TempData DELETE ALL;"
    ],
    "correct_option_index": 1,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "A table 'Products' has columns 'ProductID', 'Name', 'Price'. You need to add a primary key constraint to the 'ProductID' column. Which SQL statement is correct?",
    "options": [
      "ALTER TABLE Products ADD PRIMARY KEY (ProductID);",
      "ALTER TABLE Products MODIFY ProductID PRIMARY KEY;",
      "CREATE PRIMARY KEY ON Products (ProductID);",
      "ALTER TABLE Products SET ProductID AS PRIMARY KEY;"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "What is the primary difference between `VARCHAR` and `CHAR` data types when defining a column in a table?",
    "options": [
      "`VARCHAR` stores fixed-length strings, while `CHAR` stores variable-length strings.",
      "`VARCHAR` stores variable-length strings, while `CHAR` stores fixed-length strings.",
      "There is no difference; they are interchangeable.",
      "`CHAR` can only store numbers, while `VARCHAR` can store text."
    ],
    "correct_option_index": 1,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Which DDL command is used to create a new database schema?",
    "options": [
      "CREATE SCHEMA",
      "NEW SCHEMA",
      "MAKE SCHEMA",
      "BUILD SCHEMA"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "You have a table 'Sales' with columns 'Date', 'Product', and 'Revenue'. You want to add an index on the 'Date' column to speed up queries that filter by date. How do you do this?",
    "options": [
      "CREATE INDEX DateIndex ON Sales (Date);",
      "ALTER TABLE Sales ADD INDEX DateIndex (Date);",
      "INDEX Sales ON Date (DateIndex);",
      "NEW INDEX DateIndex ON Sales (Date);"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Which of the following is NOT a valid use of the ALTER TABLE command?",
    "options": [
      "Adding a new column",
      "Modifying a column's data type",
      "Dropping a table",
      "Renaming a table"
    ],
    "correct_option_index": 2,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "If you execute an `UPDATE` statement without a `WHERE` clause, which rows will be affected?",
    "options": [
      "No rows will be affected.",
      "Only the first row will be affected.",
      "All rows in the table will be affected.",
      "A random subset of rows will be affected."
    ],
    "correct_option_index": 2,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "How can you add a foreign key constraint to an existing table named 'Orders' that references the 'Customers' table's 'CustomerID' column?",
    "options": [
      "ALTER TABLE Orders ADD FOREIGN KEY (CustomerID) REFERENCES Customers(CustomerID);",
      "ADD FOREIGN KEY (CustomerID) REFERENCES Customers(CustomerID) TO Orders;",
      "CREATE FOREIGN KEY (CustomerID) REFERENCES Customers(CustomerID) ON Orders;",
      "LINK FOREIGN KEY (CustomerID) TO Customers(CustomerID) ON Orders;"
    ],
    "correct_option_index": 0,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "When should you use `TRUNCATE TABLE` instead of `DELETE FROM table_name;`?",
    "options": [
      "When you want to delete specific rows based on a condition.",
      "When you want to quickly delete all rows from a table and reset the identity (auto-increment) column.",
      "When you want to delete a table.",
      "When you want to update specific rows."
    ],
    "correct_option_index": 1,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "What happens if you try to insert a duplicate value into a column with a UNIQUE constraint?",
    "options": [
      "The insertion will succeed, and the duplicate value will be inserted.",
      "The insertion will fail, and an error will be raised.",
      "The duplicate value will overwrite the existing value.",
      "The database will automatically increment the duplicate value."
    ],
    "correct_option_index": 1,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "You have a table named 'Employees' with a column 'Salary' that is currently defined as INT. You need to change it to DECIMAL(10,2) to store more precise salary values. How would you do this?",
    "options": [
      "ALTER TABLE Employees MODIFY COLUMN Salary DECIMAL(10,2);",
      "ALTER TABLE Employees CHANGE COLUMN Salary DECIMAL(10,2);",
      "ALTER TABLE Employees ALTER COLUMN Salary TYPE DECIMAL(10,2);",
      "ALTER TABLE Employees UPDATE Salary TO DECIMAL(10,2);"
    ],
    "correct_option_index": 2,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Consider a table 'Products' with 'ProductID', 'Name', 'Price', and 'Category'.  You want to delete all products in the 'Discontinued' category.  However, there's a foreign key constraint linking 'Products' to another table 'OrderItems'.  How can you ensure you can delete these products?",
    "options": [
      "Disable the foreign key constraint, delete the products, then re-enable the constraint.",
      "Use the `CASCADE` option when deleting from 'Products'.",
      "Delete the corresponding rows from 'OrderItems' first, then delete the products from 'Products'.",
      "You cannot delete the products due to the foreign key constraint."
    ],
    "correct_option_index": 2,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "Which statement will remove a table named 'OldData' from the database?",
    "options": [
      "REMOVE TABLE OldData;",
      "DELETE TABLE OldData;",
      "DROP TABLE OldData;",
      "TRUNCATE TABLE OldData;"
    ],
    "correct_option_index": 2,
    "category": "SQL & Queries on DDL, DML"
  },
  {
    "question": "You have a table with millions of rows, and you need to make a schema change (e.g., adding a column).  What strategy will minimize downtime and impact on users?",
    "options": [
      "Perform the ALTER TABLE operation directly on the production table during peak hours.",
      "Take the database offline, perform the ALTER TABLE operation, then bring the database back online.",
      "Use online schema change tools or techniques (if supported by your database) to perform the alteration without significant downtime.",
      "Create a new table with the desired schema, migrate the data, and then rename the tables during a maintenance window."
    ],
    "correct_option_index": 3,
    "category": "SQL & Queries on DDL, DML"
  }
]